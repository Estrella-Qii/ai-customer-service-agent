# Roadmap

Roadmap 只记录从当前代码和测试中确认的缺口，不代表已有外部用户需求，也不承诺具体交付日期。

## v0.1.x：可靠的单实例自托管体验

1. **增加可复现的端到端评测集**
   - 为样例政策建立问题、期望来源和“不应回答”用例。
   - 在 CI 中运行不依赖付费模型的检索评测。
   - 明确检索分数含义和阈值校准方法。

2. **治理草稿的发布回滚**
   - 通过 `draft_id` 删除已发布向量。
   - 保留草稿版本和发布记录。
   - 增加 approve / rollback 的幂等测试。

3. **更可靠的文档摄取**
   - 校验 PDF 页数、加密状态和解析超时。
   - 后台处理较大文档并提供任务状态。
   - 为同名文档替换增加失败回滚，避免先删后写导致知识丢失。

## v0.2：受控团队试用

4. **身份认证与权限模型**
   - 区分提问者、知识维护者和审核者。
   - 为文档与会话增加租户边界。
   - 记录审核操作，但不保存模型密钥或原始敏感请求。

5. **生产存储与多实例安全**
   - 将治理 JSON 迁移到事务数据库。
   - 为 Redis、Qdrant 和数据库定义备份与恢复流程。
   - 增加数据保留和删除策略。

6. **可观测性与运行保护**
   - 结构化日志、请求 ID、延迟与错误率指标。
   - API 限流、超时、重试与熔断。
   - 健康检查区分关键依赖和可降级依赖。

## 暂不承诺

- 未经真实评测前不承诺回答准确率或成本收益。
- 未实现租户隔离前不承诺公网 SaaS 托管。
- 未有公开证据前不宣称用户数、下载量、生产采用或业务影响。
